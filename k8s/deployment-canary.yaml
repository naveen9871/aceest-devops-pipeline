apiVersion: apps/v1
kind: Deployment
metadata:
  name: aceest-app-canary
  labels:
    app: aceest-app
    track: canary
spec:
  replicas: 1
  selector:
    matchLabels:
      app: aceest-app
      track: canary
  template:
    metadata:
      labels:
        app: aceest-app
        track: canary
    spec:
      containers:
        - name: aceest-app
          image: naveen9871/aceest-app:canary
          ports:
            - containerPort: 5000
          readinessProbe:
            httpGet:
              path: /health
              port: 5000
            initialDelaySeconds: 5
            periodSeconds: 10
